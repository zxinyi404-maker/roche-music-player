name = "roche-netease-api"
main = "netease-proxy.js"
compatibility_date = "2024-01-01"

# Worker 类型
workers_dev = true

# 环境变量（如果需要）
[vars]
# NETEASE_API_BASE = "https://music.163.com"

# 路由（如果绑定到自定义域名）
# [[routes]]
# pattern = "api.yourdomain.com/*"
# zone_name = "yourdomain.com"
